export const environment = {
  production: true,
  /** Prefixo da API (docs/api-rest.md §12.1). */
  apiBaseUrl: '/api/v1',
  /**
   * Backend fictício. Em produção sempre desligado: os dados de demonstração
   * não podem substituir a API real por engano.
   */
  mockApi: false,
};
